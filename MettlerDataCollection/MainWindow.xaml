<Window x:Class="MettlerDataCollection.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        Title="数据采集" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Height="auto" Margin="2">
            <TextBlock Text="选择一个端口: " VerticalAlignment="Center"/>
            <ComboBox x:Name="ComportCombox" SelectedItem="{Binding SelectedComport, Mode=TwoWay}" Width="200" 
              HorizontalAlignment="Left" 
              SelectedIndex="0">
            </ComboBox>
            <Button Content="连接" MinWidth="60" Margin="2,0,2,0"/>
            <Button Content="断开" MinWidth="60" Margin="2,0,2,0"/>
            <Separator/>
            <Button Content="导出数据" MinWidth="60" Margin="2,0,2,0"/>
        </StackPanel>
        <ScottPlot:WpfPlot Grid.Row="1" Height="auto" x:Name="MainPlot" />
        <StackPanel Grid.Row="2" Height="auto" Orientation="Horizontal" Margin="2">
            <Label x:Name="ComportLabel" Content="等待连接"/>
            <Label x:Name="dataCountLabel"/>
        </StackPanel>
    </Grid>
</Window>
