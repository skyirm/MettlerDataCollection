<Window x:Class="MettlerDataCollection.Views.Help.UpdateLog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="更新日志" Height="620" Width="820" MinWidth="560" MinHeight="360"
        WindowStartupLocation="CenterOwner" Background="#F2F5FA">
    <Grid Margin="16">
        <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="12">
            <DockPanel>
                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                    <Button DockPanel.Dock="Right" Content="关闭" Width="80" Padding="8,4" Click="Close_Click" />
                    <TextBlock Text="更新日志" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
                </DockPanel>
                <TextBox x:Name="ChangelogTextBox" IsReadOnly="True" IsTabStop="True"
                         AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="13"
                         BorderThickness="0" Background="White" Padding="4" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
