<Window x:Class="MettlerDataCollection.CollectionSettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="采集设置" Height="220" Width="480"
        WindowStyle="SingleBorderWindow" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="采集设置" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />

        <TextBlock Grid.Row="1" Text="数据存储路径" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4" />

        <Grid Grid.Row="2" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" CornerRadius="2"
                    Padding="6,4" Margin="0,0,8,0" VerticalAlignment="Center">
                <TextBlock x:Name="DataPathText" Text="未设置" FontSize="12"
                           TextTrimming="CharacterEllipsis" />
            </Border>
            <Button Grid.Column="1" Content="选择文件夹" Width="90" VerticalAlignment="Center"
                    Padding="6" Click="SelectFolder_Click" />
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="80" Margin="5,0" Padding="6"
                    IsDefault="True" Click="Ok_Click" />
            <Button Content="取消" Width="80" Margin="5,0" Padding="6"
                    IsCancel="True" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
